apiVersion: apps/v1
kind: Deployment
metadata:
  name: streamlit
  namespace: recsys
  labels:
    app: streamlit
spec:
  replicas: 1
  selector:
    matchLabels:
      app: streamlit
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: streamlit
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8501"
    spec:
      containers:
        - name: streamlit
          image: recsys/streamlit:latest
          ports:
            - containerPort: 8501
          envFrom:
            - configMapRef:
                name: app-config
            - secretRef:
                name: db-credentials
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
          livenessProbe:
            httpGet:
              path: /health
              port: 8501
            initialDelaySeconds: 15
            periodSeconds: 30
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /health
              port: 8501
            initialDelaySeconds: 5
            periodSeconds: 10
